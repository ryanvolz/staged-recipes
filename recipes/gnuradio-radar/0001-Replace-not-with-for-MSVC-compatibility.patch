From c0a421d6f92ed58be95764152ef9785cb7745570 Mon Sep 17 00:00:00 2001
From: Ryan Volz <rvolz@mit.edu>
Date: Mon, 1 Jun 2020 19:26:34 -0400
Subject: [PATCH 1/3] Replace 'not' with '!' for MSVC compatibility.

---
 lib/estimator_rcs_impl.cc         | 2 +-
 lib/qtgui_scatter_plot_impl.cc    | 6 +++---
 lib/qtgui_time_plot_impl.cc       | 2 +-
 lib/scatter_plot.cc               | 2 +-
 lib/time_plot.cc                  | 2 +-
 lib/tracking_singletarget_impl.cc | 2 +-
 6 files changed, 8 insertions(+), 8 deletions(-)

diff --git a/lib/estimator_rcs_impl.cc b/lib/estimator_rcs_impl.cc
index 4e61f05..d8d53c4 100644
--- a/lib/estimator_rcs_impl.cc
+++ b/lib/estimator_rcs_impl.cc
@@ -192,7 +192,7 @@ void estimator_rcs_impl::handle_msg(pmt::pmt_t msg)
         }
     }
 
-    if (not(found_range && found_power))
+    if (!(found_range && found_power))
         throw std::runtime_error("range or power identifier (symbol) not found");
 
     d_range = pmt::f32vector_elements(d_prange);
diff --git a/lib/qtgui_scatter_plot_impl.cc b/lib/qtgui_scatter_plot_impl.cc
index f0d9c75..7a60b86 100644
--- a/lib/qtgui_scatter_plot_impl.cc
+++ b/lib/qtgui_scatter_plot_impl.cc
@@ -101,13 +101,13 @@ void qtgui_scatter_plot_impl::handle_msg(pmt::pmt_t msg)
         }
     }
     // Fill zeros in vector if only one label is found
-    if (not(item_found_x) && item_found_y) {
+    if (!(item_found_x) && item_found_y) {
         d_x.resize(d_y.size());
     }
-    if (item_found_x && not(item_found_y)) {
+    if (item_found_x && !(item_found_y)) {
         d_y.resize(d_x.size());
     }
-    if (not(item_found_x) && not(item_found_y)) {
+    if (!(item_found_x) && !(item_found_y)) {
         throw std::runtime_error(
             "All identifiers (symbols) not found"); // throw exception if no label found
     }
diff --git a/lib/qtgui_time_plot_impl.cc b/lib/qtgui_time_plot_impl.cc
index 4f40428..418a4fc 100644
--- a/lib/qtgui_time_plot_impl.cc
+++ b/lib/qtgui_time_plot_impl.cc
@@ -91,7 +91,7 @@ void qtgui_time_plot_impl::handle_msg(pmt::pmt_t msg)
         }
     }
     // Error handling
-    if (not(item_found_y)) {
+    if (!(item_found_y)) {
         throw std::runtime_error("Identifier (symbol) not found");
     }
 }
diff --git a/lib/scatter_plot.cc b/lib/scatter_plot.cc
index 3554e57..0847360 100644
--- a/lib/scatter_plot.cc
+++ b/lib/scatter_plot.cc
@@ -93,7 +93,7 @@ void scatter_plot::refresh()
     for (int k = 0; k < d_marker.size(); k++)
         d_marker[k]->detach();
 
-    if (not(*d_xy_read)) {
+    if (!(*d_xy_read)) {
         // Set new marker
         for (int k = 0; k < d_x->size();
              k++) { // len(range)==len(velocity), same size x and y!
diff --git a/lib/time_plot.cc b/lib/time_plot.cc
index d11ec59..f848b1a 100644
--- a/lib/time_plot.cc
+++ b/lib/time_plot.cc
@@ -98,7 +98,7 @@ void time_plot::refresh()
         d_marker[marker_index][k]->detach();
     }
 
-    if (not(*d_y_read)) {
+    if (!(*d_y_read)) {
         // Set new marker
         for (int k = 0; k < d_y->size(); k++) {
             if (k < d_marker[marker_index].size()) {
diff --git a/lib/tracking_singletarget_impl.cc b/lib/tracking_singletarget_impl.cc
index 8e67fe1..fd800e9 100644
--- a/lib/tracking_singletarget_impl.cc
+++ b/lib/tracking_singletarget_impl.cc
@@ -317,7 +317,7 @@ tracking_singletarget_impl::filter_particle()
 
 bool tracking_singletarget_impl::tracking()
 {
-    bool is_valid = not(d_is_empty); // measurement is valid if vectors are not empty
+    bool is_valid = !(d_is_empty); // measurement is valid if vectors are not empty
 
     // Calc time difference and matrix Q
     d_delta_t = d_time - d_time_last; // time difference
-- 
2.20.1

